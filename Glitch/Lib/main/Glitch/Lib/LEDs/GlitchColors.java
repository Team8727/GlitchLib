package Glitch.Lib.LEDs;

import edu.wpi.first.wpilibj.util.Color;

/**
 * This class is intended for the storage of all the custom colors we created for our LED patterns 
 * due to the fact that the default color options from WPILib are less than ideal in most scenarios.
 */
public class GlitchColors {
    public static Color purple = new Color(16, 0, 20);
    public static Color darkBlue = new Color(0, 0, 50);
}
